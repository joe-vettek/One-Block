package xueluoanping.oneblock.util;

public class CodeBackup {
    // var commandManager = event.getServer().getCommands();
    // var source = event.getServer().createCommandSourceStack();
    // var command = "execute if entity @e run tellraw @p \"Has Reached" + stage.getName() + "\"";
    // commandManager.performPrefixedCommand(source, command);

    // OneBlock.logger(command);


    //     // level.getBlockEntity(pos.above()).load(TagParser.parseTag("{LootTable:\"minecraft:chests/village/village_plains_house\",LootTableSeed:6194376976352609979L}")
    //     // );

    // public static void placeFeature(MinecraftServer server, BlockPos pos, ResourceLocation resourceLocation) {
    //     var commandManager = server.getCommands();
    //     var source = server.createCommandSourceStack();
    //     var command = String.format("/place feature %s %s %s %s", resourceLocation, pos.getX(), pos.getY(), pos.getZ());
    //     commandManager.performPrefixedCommand(source, command);
    // }
}
